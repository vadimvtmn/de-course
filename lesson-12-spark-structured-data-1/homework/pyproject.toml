[project]
name = "lesson-12-homework"
version = "0.1.0"
description = "L12 homework — local PySpark job over GitHub Archive events"
requires-python = "==3.12.*"
dependencies = [
  "pyspark>=4.1.1",
  "pandas>=2.2,<3",  # pandas-on-Spark (pyspark 4.1) is incompatible with pandas 3.0
  "polars>=1.40.1",
  "pyarrow>=24.0.0",
]

[dependency-groups]
dev = ["pytest>=8.3.0"]

[tool.uv]
package = false

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
